# Flowd — Claude Code Instructions

## MANDATORY: Update PROJECT.md after every change

After **every** task that modifies the codebase — no exceptions — you MUST update `PROJECT.md`:

1. Append a new entry under `## Changelog` with the format:
   ```
   ### YYYY-MM-DD — Short title
   - What was added / changed / removed
   ```
2. Update any other section of `PROJECT.md` that is now outdated (new pages, new env vars, new integrations, changed architecture, etc.)
3. Do this **before** reporting the task as complete. Never skip this step even for small changes.

## Read PROJECT.md first

At the start of every session, read `PROJECT.md` to get full context on the project before doing anything.
